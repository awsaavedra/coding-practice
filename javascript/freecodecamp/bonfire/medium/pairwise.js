/*
Return the sum of all indices of elements of 'arr' that can be paired with one 
other element to form a sum that equals the value in the second argument 'arg'. 
If multiple sums are possible, return the smallest sum. Once an element 
has been used, it cannot be reused to pair with another.
*/
//TODO: revisit problem once done, it is an interesting one
function pairwise(arr, arg) {
	sum = 0;
	
	for( var i = 0; i < arr.length; i++){
		
		for( var j = 0; j < arr.length; j++){
		}
	}
	return sum;
}

console.log(pairwise([1,4,2,3,0,5], 7));
